rust-analyzer is the most mature and full-featured editor tool for the Rust language.

rust-analyzer can be integrated with any editor which implements the Language Server Protocol (LSP).

code completions
import insertion
jumping to definitions
renaming symbols
documentation generation
refactorings
magic completions
inline compiler errors
inlay hints for types and parameters
semantic syntax highlighting
displaying inline reference documentation
