A hash set implemented as a HashMap where the value is ().

If we print each item in the HashSet, we don't know what the order will be.

useful as a collection that lets you know whether a key exists or not.
